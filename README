Build OpenWRT for Qemu on x86

	1. git clone git@github.com:sagar-kakade/openwrt_11s.git

	2. cd openwrt_11s/scripts

	3. Run the 'fetch' script

	4. cd to ~/openwrt_11s/openwrt/trunk

	5. Copy the .config file from source tree/scripts here

	6. mkdir files/etc/config

	7. Copy the configuration for wireless access points to a file called 'wireless' in files/etc/config

	8. Copy the given configuration for qemu interface in qemu-ifup
	
	9. Run the 'build' script

	10. Once OpenWRT is built successfully, open it using the 'launch' script
 
