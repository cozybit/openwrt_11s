Build OpenWRT for Qemu on x86

	1. git clone git@github.com:sagar-kakade/openwrt_11s.git

	2. cd openwrt_11s/scripts

	3. Run the 'fetch' script

	4. mkdir files/etc/config

	5. Copy the configuration for wireless access points to a file called 'wireless' in files/etc/config

	6. Copy the given configuration for qemu interface in qemu-ifup
	
	6. Run the 'build' script

	7. Once OpenWRT is built successfully, open it using the 'launch' script.
 
